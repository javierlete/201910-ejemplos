import { IluminarDirective } from './iluminar.directive';

describe('IluminarDirective', () => {
  it('should create an instance', () => {
    const directive = new IluminarDirective();
    expect(directive).toBeTruthy();
  });
});
